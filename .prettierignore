# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing

# Generator templates: EJS files (`<%= %>` placeholders) are not valid TS/JS,
# so Prettier cannot parse them. Mirrors the eslint/tsconfig `files/` excludes.
**/src/generators/**/files/
